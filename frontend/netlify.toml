[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — client-side routes must not 404 on a hard refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
